<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no, viewport-fit=cover">
    <meta name="format-detection" content="telephone=no, email=no, address=no">
    <meta name="robots" content="noindex, nofollow">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>{{ pjt.setting.name }}
      - Debug</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/musubii@7.0.0-alpha.42/docs/css/musubii.min.css">
    <style>
      .header {
        margin-bottom: 160px;
      }
      .demo-h1 {
        margin: 160px 16px 32px;
        font-size: 32px;
        font-weight: 700;
      }
      .demo-version {
        font-size: 16px;
        font-weight: 400;
      }
      .demo-h2 {
        margin: 160px 16px 32px;
        font-size: 20px;
        font-weight: 700;
      }
      .demo-h3 {
        margin: 20px 16px 8px;
        color: #828282;
        font-size: 14px;
        font-weight: 700;
      }
      .demo-h4 {
        color: #828282;
        font-size: 12px;
        font-weight: 700;
      }
      .demo-area {
        margin: 16px;
      }
    </style>
  </head>
  <body>
    <header class="header">
      <h1 class="demo-h1">
        <span>{{ pjt.setting.name }}</span>
        <span class="demo-version">v{{ pkg.version }}</span>
      </h1>
    </header>
    <main class="main">
      <h2 class="demo-h2">Example</h2>
      <div class="demo-area">
        <button type="button" class="button is-plain is-primary">View staff roll</button>
      </div>
    </main>
    <script src="./{{ pkg.name }}.js"></script>
  </body>
</html>